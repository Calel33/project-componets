/**
 * Glassmorphic Sign-In Demo
 * Showcases the animated sign-in card with all features
 */

import React from 'react';
import { GlasmorphicSignInExample } from '../ui/components/auth/USAGE_EXAMPLE';

const GlasmorphicSignInDemo: React.FC = () => {
  return <GlasmorphicSignInExample />;
};

export default GlasmorphicSignInDemo;
